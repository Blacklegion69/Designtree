import Router from "@/routes";

const App = () => {
  return (
    <div className="bg-slate-100 w-full min-h-screen flex items-center flex-col relative">
      <Router />
    </div>
  );
};

export default App;
